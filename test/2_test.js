const MetreonConfig = artifacts.require('MetreonConfig');

const AreonTokens = {
};

const AmoyTokens = {
};

const BscTokens = {
};

contract('MetreonConfig', async accounts => {
    // ========= Areon Testnet ========= //

    // it('Set Chain Token Ids Areon Testnet', async () => {

    // });

    // ========= Amoy ========= //

    // it('Set Chain Token Ids Amoy', async () => {

    // });

    // ========= Bsc Testnet ========= //

    // it('Set Chain Token Ids Bsc Testnet', async () => {

    // });
});